import Color from 'color';

const defaultColor = {
  default: '#ffffff',
  primary: '#409eff',
  success: '#67c23a',
  info: '#909399',
  warning: '#e6a23c',
  danger: '#f56c6c',
};

window.BaseColor = Color;
console.log('color', defaultColor);
